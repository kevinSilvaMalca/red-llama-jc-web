import '../styles/Spinner.css';

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="construction-message">
        Estamos construyendo por el momento
      </div>
    </div>
  );
};

export default Spinner;
